import { SpyOnMeDirective } from './spy-on-me.directive';

describe('SpyOnMeDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyOnMeDirective();
    expect(directive).toBeTruthy();
  });
});
