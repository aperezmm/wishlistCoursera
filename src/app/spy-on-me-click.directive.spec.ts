import { SpyOnMeClickDirective } from './spy-on-me-click.directive';

describe('SpyOnMeClickDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyOnMeClickDirective();
    expect(directive).toBeTruthy();
  });
});
